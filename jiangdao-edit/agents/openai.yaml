interface:
  display_name: "江导视频剪辑"
  short_description: "默认用 ChatCut；先确认完整工作流，再剪辑、交接与实播验收"
  default_prompt: "使用 $jiangdao-edit 处理这份视频。默认优先使用 ChatCut，先确认或复用完整剪辑工作流，再按已授权范围剪辑、导出并验证播放；ChatCut 不可用或我指定其他工具时，再选择对应方案。"
